[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.coverage.run]
omit = [
    "tests/*",
]

[tool.isort]
profile = "black"
skip = [
    "tests",
    "__init__.py"
]

[tool.black]
exclude = '''
/(
    tests
)/
'''
