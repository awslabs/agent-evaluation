# Test Summary
---
This document provides a summary of the tests executed by Agent Evaluation.

> ⚠️ This tool utilizes generative AI to assess virtual agents and its evaluations may contain errors. **Please thoroughly examine the results below prior to deciding whether to implement an agent.**
---

| Run ID       | Start Time           | End Time           | Number of Tests | Pass Rate      | Evaluator Input Token Count       | Evaluator Output Token Count       |
| ------------ | -------------------- | ------------------ | --------------- | -------------- | --------------------------------- | ---------------------------------- |
| {{ run_id }} | {{ run_start_time }} | {{ run_end_time }} | {{ num_tests }} | {{ pass_rate}} | {{ evaluator_input_token_count }} | {{ evaluator_output_token_count }} |

---
## Tests
{% for test in tests -%}
- [{% if test.test_result.passed %}🟢{% else %}🔴{% endif %} {{ test.name }}](#{{ test.name | replace(' ', '-') }})
{% endfor %}

---

{% for test in tests -%}
## <a id={{ test.name | replace(' ', '-') }}></a>{% if test.test_result.passed %}🟢{% else %}🔴{% endif %} {{ test.name }}

**Steps**
{% for step in test.steps -%}
{{ loop.index }}. {{ step }}
{% endfor %}

**Expected**
{% for result in test.expected.conversation -%}
{{ loop.index }}. {{ result }}
{% endfor %}

**Conversation**
```
{% for sender, message in test.test_result.messages -%}
[{{ sender }}] {{ message }}
{% endfor -%}
```

**Result**
{{ test.test_result.result }}

**Reasoning**
```
{{ test.test_result.reasoning }}
```

---
{% endfor %}





